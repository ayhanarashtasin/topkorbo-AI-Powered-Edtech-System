/**
 * BackendStatusBanner — DISABLED
 * The slow-request / cold-start banner is no longer needed.
 * Keeping the file so existing imports don't break.
 */
export default function BackendStatusBanner() {
  return null;
}
